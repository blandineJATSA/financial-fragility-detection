name: 'finprev'
version: '1.0.0'
config-version: 2

profile: 'finprev'

model-paths: ["models"]
test-paths: ["tests"]

models:
  finprev:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table